export enum Path {
  PROFILE = '/profile',
  DIALOGS = '/dialogs',
  MUSIC = '/music',
  SETTINGS = '/settings',
  NEWS = '/news',
}

export enum Types  {
  addPost = 'ADD-POST',
  updateNewPost = 'UPDATE-NEW-POST',
}